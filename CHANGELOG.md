# Changelog

## [1.1.0] - 2026-09-16

### Added

- **AI 工作实时计时**：Agent 工作期间，footer 状态行每秒刷新已耗时（跑表样式 `m:ss` / `h:mm:ss`），`agent_settled`（重试/压缩/后续消息全部处理完）后自动消失
- 与 `pi-footer` 等自定义 footer 扩展共存（其渲染可见扩展状态行时即显示计时）
- 定时器内置 `ctx.isIdle()` 兜底：异常中断路径漏触发 `agent_settled` 时自动停止，不空转；`session_shutdown` 清理定时器

### Changed

- README 重构：新增实时工作计时说明、效果示意、兼容性说明

## [1.0.1] - 2026-09-15

- 仅版本号递增（manifest 修正发布，无功能变更）

## [1.0.0] - 2026-09-15

### Added

- 用户消息时间戳：`input` 事件捕获提交时刻（FIFO 队列 + 60s 新鲜度保护），渲染在消息框内部末尾；恢复会话时按文本反查 session 条目
- 助手消息时间行：`turn_end` 时经 `pi.appendEntry()` 写入自定义条目，渲染为回复下方灰色小字（时间 + 整轮耗时，含中间工具执行）
- 仅影响显示，不改变进入 LLM 上下文的消息；时间随会话持久化